export int main() {
    int[3, 3] a = [[1, 2, 3], [4, 5, 6], [7, 8, 9]];
    int b;

    // Invalid varlet indexing
    a[0] = 3;
    a[0.0, 0] = 3;
    a[0, 0.0] = 3;
    a[0, 0, 0] = 3;

    // Invalid var indexing
    b = a[0];
    b = a[0.0, 0];
    b = a[0, 0.0];
    b = a[0, 0, 0];

    return 5;
}