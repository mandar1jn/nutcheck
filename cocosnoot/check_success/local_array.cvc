#include "civic.h"


export int main() {
    int[1, 2, 3, 4, 5, 6, 7] a = 10;
    int e = 60000;
    int b = 60000;
    int c = 60000;
    int d = 60000;

    void kaas(int[a, b, c, d, e, f, g] z) {
        printInt(a);
        printInt(b);
        printInt(c);
        printInt(d);
        printInt(e);
        printInt(f);
        printInt(g);
    }

    kaas(a);
    return 0;
}
